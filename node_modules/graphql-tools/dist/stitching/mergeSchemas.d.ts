import { GraphQLSchema, GraphQLCompositeType } from 'graphql';
import { SchemaLink } from './types';
export default function mergeSchemas({links, schemas, onTypeConflict}: {
    links?: Array<SchemaLink>;
    schemas: Array<GraphQLSchema>;
    onTypeConflict?: (leftType: GraphQLCompositeType, rightType: GraphQLCompositeType) => GraphQLCompositeType;
}): GraphQLSchema;
