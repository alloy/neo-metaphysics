"use strict";
var __assign = (this && this.__assign) || Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
        s = arguments[i];
        for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
            t[p] = s[p];
    }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
var graphql_1 = require("graphql");
function addTypenameForFragments(node, schema) {
    return graphql_1.visit(node, (_a = {},
        _a[graphql_1.Kind.SELECTION_SET] = {
            leave: function (selectionSet) {
                var selections = selectionSet.selections;
                var hasFragment = false;
                var hasTypename = false;
                selections.forEach(function (selection) {
                    if (selection.kind === graphql_1.Kind.INLINE_FRAGMENT ||
                        selection.kind === graphql_1.Kind.FRAGMENT_SPREAD) {
                        hasFragment = true;
                    }
                    else if (selection.name.value === '__typename') {
                        hasTypename = true;
                    }
                });
                if (hasFragment && !hasTypename) {
                    return __assign({}, selectionSet, { selections: selectionSet.selections.concat([
                            {
                                kind: 'Field',
                                name: {
                                    kind: 'Name',
                                    value: '__typename',
                                },
                            },
                        ]) });
                }
                return selectionSet;
            },
        },
        _a));
    var _a;
}
exports.default = addTypenameForFragments;
//# sourceMappingURL=addTypenameForFragments.js.map