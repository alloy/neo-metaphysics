import { ExecutionResult } from 'graphql';
import { GraphQLSchema } from 'graphql';
export declare type Fetcher = (operation: {
    query: string;
    operationName?: string;
    variables?: {
        [key: string]: any;
    };
}) => Promise<ExecutionResult>;
export default function addSimpleRoutingResolvers(schema: GraphQLSchema, fetcher: Fetcher): GraphQLSchema;
